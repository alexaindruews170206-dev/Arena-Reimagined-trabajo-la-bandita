[GUIDE] The stick through the forges II — F-Droid via its API, stores and sites probed (Sep 15, 2026)

**XDA · ingles-US · fuente: pieza 70 (15-sep) · verificado: 15 de septiembre de 2026**

**What it is:** the own stick's second round: F-Droid's package API answers the suggested version instantly (player.efis.pfd 8.06, soundpod 1.3.6, levyra 2.5.7), 18 store links probed alive and the doctrine: the forge is the kitchen; the store, the dining room.

**How it was verified:** F-Droid's public API + direct probe of stores and sites TODAY; forgeless projects verified by visit.

**F-Droid has a package API** (`/api/v1/packages/<app>`) and answers the suggested version instantly:

- **player.efis.pfd** — 8.06 (alive 200)
- **com.github.soundpod** — 1.3.6 (alive 200)
- **com.luc4n3x.levyra** — 2.5.7 (alive 200)

**The census's stores (probed TODAY):** the owner's archive carries **18 store and index links** outside GitHub — all probed, alive: **apt.izzysoft.de** (×3 — the sibling index that takes the apps F-Droid doesn't yet) · **flathub.org** (Komikku, alive) · **play.google.com** (boxlore, alive 200) · **accrescent.app** (alive).

**The standalone sites:** forgeless projects (briarproject.org, readest.com, kid3.kde.org, librewiki...) have no releases to sweep: **their stick is the visit** — alive (200) TODAY, with the probe's date. The census doesn't confuse them with forges: every house with its own trade.

**Is a store a forge?** No. The forge keeps the code; the store keeps the packages — whoever audits code isn't reading the same thing as whoever installs an APK. **The forge is the kitchen; the store, the dining room.** Confusing them is confusing the recipe with the dish.
## Changelog

- **Sep 15, 2026:** F-Droid versions answered by API on cutoff day.

## Credits and links

Thread 70 of the series: "The stick through the forges II — F-Droid via its API, stores and sites probed". Every project links to its home on GitHub — full author/repo path, no shorteners. This thread's cutoff — "The stick through the forges II — F-Droid via its API, stores and sites probed" — was read at its home on Sep 15; if a number changes, thread 70 gets edited with a date — never in silence. The trade's meme:

> —Are the store and the forge the same thing?
> —The forge is the kitchen; the store, the dining room. The dish can be the same — the recipe, no.

*La Bandita verifies before recommending: stars decorate; dates decide.*